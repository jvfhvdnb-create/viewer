import "./style.css";
const app=document.querySelector("#app");app.innerHTML=`<main class="shell"><header><div><small>IMAGEBOARD LAN</small><h1>لوحة الأدمن</h1><p>اسحب الصورة وأسقطها لإرسالها مباشرة إلى شاشة العرض.</p></div><div class="status"><i id="dot"></i><span id="status">في انتظار Viewer</span></div></header><section id="drop" class="drop"><div class="plus">＋</div><h2>اسحب الصورة هنا</h2><p>أو اختر صورة من Windows</p><button id="pick">اختيار صورة</button><input id="file" type="file" accept="image/*" hidden></section><section class="info"><div><label>Viewer</label><strong id="viewer">غير متصل</strong></div><div><label>عنوان الأدمن</label><strong id="ip">...</strong></div><div><label>WebSocket</label><strong>TCP 8765</strong></div></section><div id="msg">جاهز</div></main>`;
const drop=document.querySelector("#drop"),pick=document.querySelector("#pick"),file=document.querySelector("#file"),msg=document.querySelector("#msg");let connected=false,busy=false;function status(v){connected=v;document.querySelector("#dot").className=v?"on":"";document.querySelector("#status").textContent=v?"Viewer متصل":"في انتظار Viewer";document.querySelector("#viewer").textContent=v?"🟢 متصل":"🟡 غير متصل"}async function send(f){if(!f||busy)return;if(!f.type.startsWith("image/")){msg.textContent="الملف ليس صورة";return}if(!connected){msg.textContent="لا يوجد Viewer متصل";return}busy=true;pick.disabled=true;msg.textContent="جارٍ الإرسال...";try{const b=await f.arrayBuffer(),r=await window.imageBoard.sendImage(b,f.name,f.type);msg.textContent=r.sent?`تم إرسال ${f.name} إلى ${r.count} شاشة`:"لم يتم الإرسال"}catch(e){msg.textContent=`فشل الإرسال: ${e.message||e}`}finally{busy=false;pick.disabled=false}}pick.onclick=()=>file.click();file.onchange=()=>{send(file.files?.[0]);file.value=""};["dragenter","dragover"].forEach(x=>drop.addEventListener(x,e=>{e.preventDefault();drop.classList.add("active")}));["dragleave","drop"].forEach(x=>drop.addEventListener(x,e=>{e.preventDefault();drop.classList.remove("active")}));drop.ondrop=e=>send(e.dataTransfer.files?.[0]);window.imageBoard.onStatus(status);window.imageBoard.getAddress().then(x=>document.querySelector("#ip").textContent=x);status(false);