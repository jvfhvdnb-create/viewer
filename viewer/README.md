# Viewer
Viewer opens fullscreen/kiosk immediately, discovers Admin automatically on LAN, reconnects automatically, and enables Windows startup after first launch. It does not save received images as files. Build MSI with `npm run dist:msi`.
